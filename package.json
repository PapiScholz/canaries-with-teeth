{
  "name": "canaries-with-teeth",
  "version": "1.0.0",
  "description": "Zero-config deterministic canary, telemetry, risk, and gating system.",
  "bin": {
    "canaries": "dist/cli.js"
  },
  "scripts": {
    "canaries:init": "npx canaries init",
    "canaries:ci": "npx canaries ci",
    "canaries:risk": "npx canaries risk",
    "canaries:gate": "npx canaries gate",
    "canaries:dashboard": "npx canaries dashboard"
  },
  "type": "module",
  "dependencies": {
    "playwright": "^1.41.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.1",
    "@types/node": "^25.1.0",
    "typescript": "^5.9.3"
  }
}
